<div class="customer">
  <h2><%= customer.full_name %></h2>
  <%= image_tag(customer.image) %>
  <p><strong>Province: </strong><%= customer.province == nil ? 'None' : customer.province.name %></p>
  <p><%= customer.phone_number %></p>
  <p><%= customer.email %> </p>
</div>
